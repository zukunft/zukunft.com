# only debian at the moment
OS=debian
# path to the www root folder
WWW_ROOT=/var/www/html
# path to the git repository (objects + history); MUST be outside WWW_ROOT so the
# clone's .git cannot be downloaded over the web (see script/install.sh)
ZUKUNFT_GIT_DIR=/var/lib/zukunft.git
# prod, test or develop
ENV=dev
# master (for prod), release (for test) or develop (for dev)
BRANCH=develop
# postgres suggested but MySQL
DB=postgres
# the name of this pod which is also used to create the url
POD_NAME=zukunft.com
# the base url of this server / pod e.g. 'https://www.zukunft.com/', 'https://test.zukunft.com/', 'http://localhost/', ...
THIS_URL=http://your-server-name/
# base url for a speperate server for the system log; if empty the base server is used
SYS_LOG_URL=
# the git repository the program updates are pulled from (shown on the server admin page)
SOURCE_REPO_URL=https://github.com/zukunft/zukunft.com

PGSQL_DATABASE=zukunft
PGSQL_USERNAME=zukunft
PGSQL_PASSWORD=change_me
# only used for script base recreation of the zukunft db user
# PGSQL_ADMIN_DATABASE=postgres
# PGSQL_ADMIN_USERNAME=postgres
# PGSQL_ADMIN_PASSWORD=
PGSQL_HOST=localhost
PGSQL_PORT=5432

MYSQL_DATABASE=zukunft
MYSQL_USERNAME=zukunft
MYSQL_PASSWORD=your_secure_password
# only used for script base recreation of the zukunft db user
# MYSQL_ADMIN_DATABASE=mysql
# MYSQL_ADMIN_USERNAME=root
# MYSQL_ADMIN_PASSWORD=
MYSQL_HOST=localhost
MYSQL_PORT=5432

# use the database of a file folder as cache
CACHE=database
CACHE_TIME='-1 day'# max age of a data cache

ZUKUNFT_ALLOW_SETUP=1# switch this off after the initial setup

# server admin
# the os user the web server runs as; used for the sudo rule that lets the server admin page run script/server_admin.sh
WEB_USER=www-data
# comma separated IPs / CIDR ranges allowed to reach the server admin page and the fixed IP of the main system admin (other admins do not need a fixed ip); falls back to localhost
SERVER_ADMIN_IP=94.130.31.152
SERVER_ADMIN_USER=admin
SERVER_ADMIN_PW=hash_of_change_me
SERVER_ADMIN_2_USER=admin2
SERVER_ADMIN_2_PW=hash_of_change_me
SERVER_ADMIN_2_IP_FROM=77.59.0.0
SERVER_ADMIN_2_IP_TO=77.59.255.255
SERVER_ADMIN_3_USER=admin3
SERVER_ADMIN_3_PW=hash_of_change_me
SERVER_ADMIN_3_IP_FROM=
SERVER_ADMIN_3_IP_TO=
# public admin contact email shown to rejected visitors on the whitelist reject pages; empty falls back to ADMIN_MAIL
SERVER_ADMIN_MAIL=admin@your-server-name.tld

# data admin
# the *_PW login passwords below are only used once at setup to create the initial users (stored
# bcrypt-hashed in the database); install.sh then blanks them in the deployed .env, which itself is
# kept one level above the web root so the secrets are never inside the docroot
ADMIN_USER=admin#can be set here or will be ask at initial start
ADMIN_PW=#can be set here or will be ask at initial start
ADMIN_MAIL=admin@zukunft.com
CO_ADMIN_USER=deputy#can be set here or will be ask at initial start
CO_ADMIN_PW=#can be set here or will be ask at initial start
CO_ADMIN_MAIL=successor@zukunft.com

# test user
USER_NAME=vr#can be set here to create an initial user for testing
USER_PW=#can be set here or will be ask at initial start
USER_MAIL=vr@zukunft.com
CO_USER_NAME=management#can be set here to create an initial partner user for testing
CO_USER_PW=#can be set here or will be ask at initial start
CO_USER_MAIL=management@zukunft.com
